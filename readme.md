Oregon wildfire predictive model

Jacob Seiberg, Ben Elster, Reid Allenstein, Eleanor Moseley

### Setup
This repo uses Git LFS for CSV files.
Before cloning, install Git LFS:
1. Install from https://git-lfs.github.com/
2. Run `git lfs install`
3. Then clone the repo normally 